import 'package:flutter/material.dart';

class SpaceTiles extends StatelessWidget {
  const SpaceTiles({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(children: [
      Divider(
        thickness: 0.4,
      ),
      SizedBox(
        height: 40,
      )
    ]);
  }
}
